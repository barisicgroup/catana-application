describe('catana/coarse grained data model', function () {
    it('initialization', function () {
        //const newStruc : CgStructure = new CgStructure(1, "test-structure", "test-author");
        // ---
        // Currently not possible to write tests because the jest fails with an error
        // "SyntaxError: Unexpected identifier" involving shaders (Mesh.vert) included in mesh-buffer.
        // Several different potential solutions tried but none worked. :'(
    })
})